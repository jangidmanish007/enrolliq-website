import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800", "900"],
  display: "swap",
});

export const metadata = {
  title: "EnrollIQ — Intelligent Admissions & CRM Platform",
  description:
    "EnrollIQ is the AI-powered CRM and enrolment management platform built for modern educational institutions. Integrate with Meritto, HubSpot, Salesforce, LeadSquared and more.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col font-[var(--font-inter)]">
        {children}
      </body>
    </html>
  );
}
